apiVersion: v1
kind: Service
metadata:
  labels:
    name: ingress-admission
  name: ingress-admission
spec:
  selector:
    name: ingress-admission
  ports:
  - name: https
    port: 443
    targetPort: 8443
